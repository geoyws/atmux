# 2026-05-17

## Shipped (kanban→done)

(appended by gitter post-fan-in, or hygiene-tick backstop)

## Merges (branch→trunk)

(appended by gitter post-trunk-merge per ADR-145 + ADR-146)

## ADRs landed

(appended by hygiene-tick on detecting new docs/adr/*.md, OR by ADR author)

## Complaints adjudicated

- c-2764c094 → **wontfix** (4th recurrence in <48h of medic/superdoctor-misroutes-claude-skills-to-targetTeam=atmux pattern; prior: c-af0c517f / c-d2bdc42a / c-cbc8f909 on 2026-05-16. `openedBy=medic` now (per ADR-133 superdoctor→medic rename) but same root content as c-d2bdc42a + c-cbc8f909 — cited surface is `superdoctor-prompt.md §2.5a` bash probe, **claude-skills dotfiles territory** per `[[feedback_ombudsman_claude_skills_wontfix]]` + `[[feedback_claude_skills_dotfiles_territory]]`; atmux team cannot touch. Complaint body itself explicitly confirms atmux-side is correct: *"The pure-function verdictForCage in `src/core/superdoctor-cage-verdict.ts` already accepts a boolean sessionAlive; the change is at the caller (skill brief inline-table)"*. Ground-truth check at adjudication: complaint cites "atmux 5 commits last 1h, unum 13 commits last 1h" → teams ARE shipping; this is a probe-reporting-false-DEAD vs reality, NOT an actual cage-down condition. Routing fix at medic prompt is also dotfiles; ombudsman adjudication signal is the only escalation surface available. **4-in-a-row strengthens prior pattern flag**: operator should fix the medic prompt to route claude-skills bugs into the dotfiles flow, not the atmux complaints DB.)
- c-67bbac0a → **filed EPIC e-38ee9939** (openedBy=medic, targetTeam=atmux, kind=improvement, severity=medium. JSON→SQLite migration incomplete: ~14 state-of-record JSON files remain under `.atmux/state/` despite ADR-126 (Status: proposed, 2026-05-07) establishing the SQLite-canonical pattern and the April-2026 kanban migration shipping the template. Operator-framing quoted in body: *"why are we still using JSON when we have SQLite?"*. Filed as EPIC (not task) because body sketches a 3-phase decomp: **P1** per-flag single-row toggles → `flags` table (6 files); **P2** per-role tracking → `role_state` table (12+ files including ombudsman-pending.json itself); **P3** budget state → `budget` table (3 files). Body explicitly lists KEEP-AS-JSON carve-outs per ADR-126 §"What this ADR is NOT" (cockpit.json + team.json + .claude/team.json + budget-probe-*.json + budget-history.json). Cross-refs ADR-126 (principle, proposed) + ADR-154 sibling EPIC t-2298cbb0 (driver-inbox/lead-outbox markdown→SQLite — different surface but same SQLite-canonical principle) + ADR-076 precedent (inboxes shipped). Hard guardrail per ADR-077 §7d — medic files but does NOT implement. Routes to planner; lead-outbox pinged per §OQ2 default (epic filing).)
- c-27a1c8f4 → **filed task t-97044284** (openedBy=medic, targetTeam=atmux, kind=improvement, severity=medium. `tests/unit/verbs/cockpit.test.ts:308` `spinTmux` helper leaks `/tmp/atmux-cockpit-<prefix>-XXXXXX` tmux servers + zombie claudes when bun-test parent SIGKILL'd — JS-level cleanup hooks (`try/finally` + `afterAll` + `process.on('exit')`) don't fire. Concrete: 6 orphan servers + ~700MB RSS waste observed by medic May 14-16. Filed task for the **in-process half** (`.leak-tracker.json` sidecar in spinTmux + new `atmux test-reaper` verb that globs `/tmp/atmux-cockpit-*-*` and reaps when parentPid dead + age > max-age-min, with optional `bunfig.toml` preload hook). Out-of-process half (medic §0-prelude sweep) is **dotfiles territory** per `[[feedback_claude_skills_dotfiles_territory]]` — operator handles separately. In-process alone closes the leak per complaint body ("Either alone closes the leak; both together is belt-and-suspenders"). NOT epic — atomic single-feature fix; routes to planner.)

## Doctor regressions (optional)

(appended by medic on red-row escalation; empty most days)

## Notes (optional)

(operator-curated narrative; empty most days)
