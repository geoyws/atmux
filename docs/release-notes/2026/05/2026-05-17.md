# 2026-05-17

## Shipped (kanban→done)

(appended by gitter post-fan-in, or hygiene-tick backstop)

## Merges (branch→trunk)

(appended by gitter post-trunk-merge per ADR-145 + ADR-146)

## ADRs landed

(appended by hygiene-tick on detecting new docs/adr/*.md, OR by ADR author)

## Complaints adjudicated

- c-2764c094 → **wontfix** (4th recurrence in <48h of medic/superdoctor-misroutes-claude-skills-to-targetTeam=atmux pattern; prior: c-af0c517f / c-d2bdc42a / c-cbc8f909 on 2026-05-16. `openedBy=medic` now (per ADR-133 superdoctor→medic rename) but same root content as c-d2bdc42a + c-cbc8f909 — cited surface is `superdoctor-prompt.md §2.5a` bash probe, **claude-skills dotfiles territory** per `[[feedback_ombudsman_claude_skills_wontfix]]` + `[[feedback_claude_skills_dotfiles_territory]]`; atmux team cannot touch. Complaint body itself explicitly confirms atmux-side is correct: *"The pure-function verdictForCage in `src/core/superdoctor-cage-verdict.ts` already accepts a boolean sessionAlive; the change is at the caller (skill brief inline-table)"*. Ground-truth check at adjudication: complaint cites "atmux 5 commits last 1h, unum 13 commits last 1h" → teams ARE shipping; this is a probe-reporting-false-DEAD vs reality, NOT an actual cage-down condition. Routing fix at medic prompt is also dotfiles; ombudsman adjudication signal is the only escalation surface available. **4-in-a-row strengthens prior pattern flag**: operator should fix the medic prompt to route claude-skills bugs into the dotfiles flow, not the atmux complaints DB.)

## Doctor regressions (optional)

(appended by medic on red-row escalation; empty most days)

## Notes (optional)

(operator-curated narrative; empty most days)
