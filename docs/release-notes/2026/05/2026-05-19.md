# 2026-05-19

## Shipped (kanban→done)

(appended by gitter post-fan-in, or hygiene-tick backstop)

## Merges (branch→trunk)

(appended by gitter post-trunk-merge per ADR-145 + ADR-146)

## ADRs landed

(appended by hygiene-tick on detecting new docs/adr/*.md, OR by ADR author)

## Complaints adjudicated

- c-238f0faf → **filed task t-237d2c3c** (openedBy=george-interactive, sourceKind=operator, targetTeam=atmux, kind=test-fixture-leak, severity=medium. 9 dead tmux sockets observed at `/tmp/tmux-0/` (operator's default `$TMUX_TMPDIR=/tmp` socket dir) from atmux test fixtures: `atmux-cockpit-rotate-t7`, `atmux-conf-test`, `atmux-e2e`, plus generic playground names (`s`, `sk2`, `sock1`, `sx`, `test-bun`, `test-iso`). **Distinct leak class from c-27a1c8f4 / t-97044284** (already closed via ADR-178) — that one addresses `/tmp/atmux-cockpit-<prefix>-XXXXXX` ephemeral-tmpdir leaks with `.leak-tracker.json` sidecar; this addresses leaks where tests call `tmux -L <name>` WITHOUT setting `TMUX_TMPDIR` so sockets land in the operator's default dir (which the reaper's `<tmpdir>/atmux-cockpit-*-*` glob doesn't reach + tests don't write sidecars). Asks: (a) audit `tests/` for `tmux -L|new-session` without ephemeral `TMUX_TMPDIR` override, (b) retrofit ephemeral-tmpdir + sidecar to all test surfaces, (c) reaper-glob widening fallback (`--prefix` list + secondary `/tmp/tmux-*/atmux-*` scan), (d) `atmux doctor` probe for `atmux-*` sockets with 0 sessions in default tmpdir. NOT epic — scoped follow-up to closed ADR-178; routes to planner.)
- c-ced218fb → **filed EPIC e-59b43669** (openedBy=george-interactive, sourceKind=operator, targetTeam=atmux, kind=orphan-cron, severity=high. 10 orphan `atmux:team=X` whip-cron blocks survive team teardown — `crontab -l | grep -oE 'atmux:team=[^ ]+'` returns 27 entries but cockpit.json roster only 17 (5 parent + medic + 12 epic). Orphans: 2 parent-team (`acme`, `new`) + 8 epic-team (`e-2c1ac890`, `e-2c6ed4b2`, `e-4d8c0b12`, `e-a25968cc`, `e-a3077ca0`, `e-aeaf7ed4`, `e-fa58a2f9`, `e-fbef65d7`). Recurring class — same root cause as the test-fixture socket leaks (SIGKILL'd parents skip JS cleanup hooks) plus structural gap (`team dissolve-epic` + `team sweep-epics --apply` don't remove their cron blocks). Impact: burns budget firing whip against non-existent teams + hides real dormancy + pollutes operator `crontab -l`. **EPIC scope**: new `atmux cron-reaper [--dry-run] [--apply]` verb + groom-cron wire (04:00 daily phase) + teardown-hook updates on `dissolve-epic`/`sweep-epics`/`stop --soft|--force` (idempotent best-effort remove) + `atmux doctor` orphan-cron probe + regression test (spawn 2 / dissolve 1 / assert clean cron state). Decomposition hint: 5-6 tasks per planner (T1 ADR draft / T2 reaper verb / T3 teardown hooks / T4 groom wire / T5 doctor probe / T6 e2e). Routes to planner; lead-outbox pinged per §OQ2 default — epic filings only.)

## Doctor regressions (optional)

(appended by medic on red-row escalation; empty most days)

## Notes (optional)

(operator-curated narrative; empty most days)
