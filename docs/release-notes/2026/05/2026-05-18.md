# 2026-05-18

## Shipped (kanban→done)

(appended by gitter post-fan-in, or hygiene-tick backstop)

## Merges (branch→trunk)

(appended by gitter post-trunk-merge per ADR-145 + ADR-146)

## ADRs landed

(appended by hygiene-tick on detecting new docs/adr/*.md, OR by ADR author)

## Complaints adjudicated

- c-fccbc6cc → **filed task t-f49fa132** (sourceKind=whip, targetTeam=atmux, kind=verb-bug, severity=p2. `atmux rotate <member>` 0.8.4 fails with `no tmux window for docs` on the parent atmux team's 6-core coordination roster. **Live repro verified at adjudication time** on `/root/work/src/atmux/.atmux/tmux/tmux-0/default`: windows `🦦_docs` (underscore) and `🌿-gitter-` (hyphen) drift from `buildWindowName` logic. **Root cause** (`src/core/common.ts` DEFAULT_MEMBER_ROLES + `src/verbs/rotate.ts:270`): ADR-161 TR2 (2026-05-16, commit `5b5981d`) shipped role-aware `_-prefix` for 4 roles `[team-lead, planner, reviewer, ombudsman]`; same-day 6-core decomp (per `[[project_atmux_team_decomposed_to_core]]`) added `docs` + `committer` (gitter's ADR-159 role) but the constant was not extended. `isDefaultMemberRole("docs")` → false → `buildWindowName` returns `🦦-docs` (hyphen) → `windowExists` misses the actual `🦦_docs` window → "no tmux window for docs". Task body offers 3 fix options (recommend (a): extend DEFAULT_MEMBER_ROLES to match 6-core; single-line change with downstream paths inheriting fix). NOT epic — atomic bug fix; routes to planner.)
- c-abb7b603 → **filed task t-03c2c2e8** (openedBy=superdoctor, targetTeam=atmux, kind=incident, severity=high. `atmux cockpit rebuild` flattens epic-team viewer windows into the cockpit session, regressing ADR-089 §Pillar 1 §Amendment (ba1f1c1, 2026-05-16) which moved epic-team viewer placement to the parent cage via `addEpicViewerToParentCage` (called from `src/verbs/start.ts:967`). **Root cause**: `enabledTeams()` at `src/core/cockpit.ts:437` returns BOTH `type: "team"` and `type: "epic-team"` flattened entries in DFS pre-order; cockpit rebuild Phase 5 (`src/verbs/cockpit.ts:510` + `:588`) passes that flat list to `reconcileCockpitSession` → every epic-team gets a cockpit-tier viewer window intermixed with parent teams. Observed 2026-05-18 14:39 MYT on `atmux_cockpit`: 24 flat windows, 18 of them epic-team viewers (e.g. e-a25968cc=window 17 between rentx and sopx-epics, no spatial relation to its `atmux` parent at window 3). **Collision context**: f622c3d (ADR-135 §D2 §Amendment, 18:16 MYT, 15min BEFORE ba1f1c1 at 18:31 MYT) added a positional sweep in cockpit rebuild that ordered epic-team viewers DFS-adjacent to their parent in the cockpit; ba1f1c1 supersedes that placement model but didn't update the rebuild verb to stop creating the windows in the first place. Filed task (not EPIC) — atomic scoped fix: filter `type === "team"` at the rebuild call-site, restrict f622c3d positional sweep to top-level teams, regression test against a cockpit.json with at least one configured epic-team, ADR-135 §D2 §Amendment annotation pointing to ADR-089 §Pillar 1 §Amendment as the superseding rule for epic-team viewer placement. Routes to planner; lead-outbox not pinged (§OQ2 default — epics only, not tasks).)
- c-60b89057 → **already-addressed** (cross-team complaint filed by george from sopx-root targeting atmux, sourceKind=operator, kind=feature-gap, severity=P2. Asked for `atmux task add --epic <eid>` + `atmux task update --epic <eid|->` flags so epic-team-dispatched tasks can be tagged with their epic id (kanban schema has `tasks.epic` column but no verb exposes it for write). **Already covered by ADR-172 chain**: T1 ADR draft `t-c8cf13c2` done, T2 `task.ts add` flag restore `t-e87f3ee0` queued, T3 `task.ts update` flag restore `t-f7eb6c4d` queued, T4 integration test `t-3c22ec52` queued; sibling ADR-174 covers `task list --epic` filter (`t-86e78acc` done, `t-66d6cd14` queued). Older precursor `t-66746ab4` from same operator session also exists. No new filing needed — resolve points sopx driver to the queued impl chain.)
- c-784b1570 → **wontfix** (cross-team complaint filed by claude-driver from sopx-root targeting atmux, sourceKind=operator, kind=product-bug, severity=low. Reported `/bau 1` reporting "Stuck-input · N queued member panes" on sopx members that were actually polling on ScheduleWakeup loops (mid-cycle ✻ indicators visible in scrollback). Asked for `scripts/lib-velocity-gate.sh` patch + `tests/velocity-gate.bats` regression. **Path lives in claude-skills dotfiles territory** (`/root/work/journals/.sb/claude-skills/plugins/coordination/skills/bau/scripts/lib-velocity-gate.sh`). Per memory rules `[[feedback_claude_skills_dotfiles_territory]]` + `[[feedback_ombudsman_claude_skills_wontfix]]`: atmux team must not touch claude-skills; operator-managed via dotfiles flow. Route to operator dotfiles edit, not atmux kanban.)

## Doctor regressions (optional)

(appended by medic on red-row escalation; empty most days)

## Notes (optional)

(operator-curated narrative; empty most days)
