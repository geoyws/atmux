# 2026-05-18

## Shipped (kanban→done)

(appended by gitter post-fan-in, or hygiene-tick backstop)

## Merges (branch→trunk)

(appended by gitter post-trunk-merge per ADR-145 + ADR-146)

## ADRs landed

(appended by hygiene-tick on detecting new docs/adr/*.md, OR by ADR author)

## Complaints adjudicated

- c-fccbc6cc → **filed task t-f49fa132** (sourceKind=whip, targetTeam=atmux, kind=verb-bug, severity=p2. `atmux rotate <member>` 0.8.4 fails with `no tmux window for docs` on the parent atmux team's 6-core coordination roster. **Live repro verified at adjudication time** on `/root/work/src/atmux/.atmux/tmux/tmux-0/default`: windows `🦦_docs` (underscore) and `🌿-gitter-` (hyphen) drift from `buildWindowName` logic. **Root cause** (`src/core/common.ts` DEFAULT_MEMBER_ROLES + `src/verbs/rotate.ts:270`): ADR-161 TR2 (2026-05-16, commit `5b5981d`) shipped role-aware `_-prefix` for 4 roles `[team-lead, planner, reviewer, ombudsman]`; same-day 6-core decomp (per `[[project_atmux_team_decomposed_to_core]]`) added `docs` + `committer` (gitter's ADR-159 role) but the constant was not extended. `isDefaultMemberRole("docs")` → false → `buildWindowName` returns `🦦-docs` (hyphen) → `windowExists` misses the actual `🦦_docs` window → "no tmux window for docs". Task body offers 3 fix options (recommend (a): extend DEFAULT_MEMBER_ROLES to match 6-core; single-line change with downstream paths inheriting fix). NOT epic — atomic bug fix; routes to planner.)

## Doctor regressions (optional)

(appended by medic on red-row escalation; empty most days)

## Notes (optional)

(operator-curated narrative; empty most days)
