# 2026-05-16

## Shipped (kanban→done)

(appended by gitter post-fan-in, or hygiene-tick backstop)

## Merges (branch→trunk)

(appended by gitter post-trunk-merge per ADR-145 + ADR-146)

## ADRs landed

(appended by hygiene-tick on detecting new docs/adr/*.md, OR by ADR author)

## Complaints adjudicated

- c-7a308f7f → **filed task t-82b6aed9** (groom: --inbox-days flag is reserved-but-unwired; ## Open never ages → ## Archive — surfaced concrete bloat on sopx lead-outbox.md = 10668 lines. Filed as standalone task, NOT epic, since complaint body sketches a single ~80-LOC sub-op `ageInboxOpenToArchive` + 3 fixture tests = 1-2 days work. Task body cross-refs ADR-154 EPIC t-2298cbb0 (markdown→SQLite migration, proposed) and lays out (a) ship stopgap / (b) defer / (c) hybrid one-shot — planner judgment call. Routed to planner.)
- c-33f7531a → **already-addressed** (whip-velocity-gate p0 source; ADR-087 T2 t-e91fec98 SHIPPED is the canonical escalation pipeline → medic structural-fix verbs rotate-lead / kill+respawn / account-swap. Filed at ~12:30 MYT when trunk had 0 commits / 71min stale. Post-file reality at adjudication time 17:00 MYT: team self-recovered with **18 commits across branches in the 5h window**, gitter fanned 1 to trunk; no structural intervention needed by the time medic would have woken. **Meta-flag for operator/driver**: no `medic` / `superdoctor` process observed on host at adjudication time — verify cockpit W2 per ADR-133 is actually running; if absent, future velocity-gate complaints will accumulate without auto-action and atmux team will rely on lead self-recovery, which is exactly the failure mode ADR-087 was designed to bypass. Future whip-velocity-gate complaints with the same symptom hash adjudicate identically unless the cockpit-medic side flips state.)
- c-cef89cfd → **resolved (duplicate-of t-ccabd763)** (sourceKind=operator, targetTeam=atmux, kind=ux-gap, severity=medium. Complaint and task were filed together by operator 2026-05-16 — task body already carries full implementation sketch (`quiesceCron` helper in `src/core/soft-stop.ts`, 5-step ordering, ADR-087 §D4 extension proposal), test plan (unit + integration + e2e dogfood on atmux team itself), refs to ADR-053 advisory-locking + ADR-076 per-team cron isolation. Pointer-only resolve via `--related-task t-ccabd763`; no new task filed — would be a literal duplicate. **Meta-flag for operator: duplicate ADR-087 numbering** — `docs/adr/087-atmux-stop-soft.md` (Accepted 2026-05-13) AND `docs/adr/087-whip-velocity-gate.md` (proposed 2026-05-14) both exist; "ADR-087" is ambiguous in code comments + briefs + commit messages. Violates project CLAUDE.md "monotonically-numbered tree" convention. The complaint's "ADR-087" cite resolves correctly (the soft-stop one) by context, but mechanical lookups will hit the wrong file ~50% of the time. Worth a separate cleanup task — out-of-scope for this adjudication, surfaced here for operator/driver pickup.)
- c-003a2a4c → **filed task t-8dbbf05a** (sourceKind=operator, kind=regression, severity=high. atmux 0.8.0 install bundle ships only bin/, omits templates/ → `atmux init` errors with `fs read failed on /templates/team.example.json` for any user running the installed `/usr/local/bin/atmux`. Live repro verified at adjudication time on /opt/atmux/0.8.0/. Complaint pre-linked to t-2b801707 (the 71-test-fail meta-task) but that task bundles 15 clusters — operator's own body explicitly noted "filing a separate complaint shortly" signalling they want this carved out. Carved cluster #13 into a scoped task: 1-line bundler change + post-install verify gate in the 0.8.0 release-event runbook (would have caught it). Re-pointed `--related-task` to the new task. NOT epic — atomic fix. severity=high reflects user-facing impact: atmux init is the open-source onboarding entry-point for 0.8.0.)
- c-0b42bd14 → **already-addressed** (whip-velocity-gate p0; 2nd recurrence of `whip-vg-atmux-velocity-bad` symptom hash in 8h on atmux team. ADR-087 T2 t-e91fec98 SHIPPED is the canonical pipeline; team self-recovered at adjudication time — `eb7f5ee` (planner merge ADR-158/159/160/161/162) landed **61 seconds before adjudication**. Strikes file cleared per ADR-087 §5 (file-on-3rd-strike then reset). **New signal vs c-33f7531a**: filing carries `Lead saturated: 1` and `Lead tokens: 260209` (~26% of 1M ctx, just under the 30% rotation threshold per `[[feedback_rotation_threshold_400k]]`). **Recurrence pattern surfaced to operator**: two velocity-bad complaints in 8h on the same team + the new saturation indicator suggests the lead-rotation cadence (CLAUDE.md "Lead auto-rotates at 60min uptime", Whip §1a gate on `~/.claude/teams/atmux/lead-session-start.txt`) is not firing. Still no `medic` / `superdoctor` process observed (only test artifacts + an interactive `atmux_cockpit` attach). Operator: verify cockpit W2 medic AND lead-rotation gate are actually wired on this team. Same adjudication will repeat on every future filing of this symptom hash until the underlying cadence flips.)
- c-af0c517f → **wontfix** (openedBy=superdoctor, targetTeam=atmux. Cited surface is claude-skills `superdoctor-prompt.md §3a` — **dotfiles territory** per `[[feedback_claude_skills_dotfiles_territory]]`, atmux team must NOT touch. Underlying capability already shipped: t-d98b2bd6 DONE 2026-05-15 with atmux 459d2d4 (status ctx % column + 15 tests) AND claude-skills 0478fd8 (`measure-context.sh` + 22-test suite). Residual gap (4 cockpit teams have empty `~/.claude/teams/<team>/member-context/` dirs = idle-hook never wrote JSON) is dotfiles rollout / member-side hook invocation — operator-managed. Filing an atmux task for this would be a role-overreach. targetTeam=atmux is wrong routing.)
- c-d2bdc42a → **wontfix** (openedBy=superdoctor, targetTeam=atmux. Cited surface is claude-skills `superdoctor-prompt.md §2.5a` bash probe — **dotfiles territory** per `[[feedback_claude_skills_dotfiles_territory]]`. Complaint body explicitly confirms "classifier itself needs no change — input synthesis does" — atmux-side `src/core/superdoctor-cage-verdict.ts` is correct, only the prompt-embedded probe is wrong. Operator handles dotfiles. targetTeam=atmux is wrong routing.)
- **Meta-pattern across c-af0c517f + c-d2bdc42a**: `superdoctor` (the cockpit role) is filing claude-skills bugs against `targetTeam=atmux`. The routing itself is a superdoctor-side issue (the role doesn't know its own complaints should target the dotfiles flow, not the team whose name happens to match `targetTeam` semantics). The fix is in the superdoctor prompt (also dotfiles); ombudsman cannot file it. Surfaced here so operator sees the recurrence — a third superdoctor-filed claude-skills complaint will adjudicate identically until the superdoctor-side routing flips.

## Doctor regressions (optional)

(appended by medic on red-row escalation; empty most days)

## Notes (optional)

(operator-curated narrative; empty most days)
