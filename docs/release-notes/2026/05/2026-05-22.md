# 2026-05-22

## Shipped (kanban→done)

(appended by gitter post-fan-in, or hygiene-tick backstop)

## Merges (branch→trunk)

(appended by gitter post-trunk-merge per ADR-145 + ADR-146)

## ADRs landed

(appended by hygiene-tick on detecting new docs/adr/*.md, OR by ADR author)

## Complaints adjudicated

- c-88f1d864 → **already-addressed** (sopx-driver, severity=med, design-gap: "Epic kanban needs 'done' lifecycle". Premise incorrect: `epic.status === "done"` exists per `src/core/epic.ts:21` canonical machine `planning→ready→in-progress→review→done`; ADR-091 §EPIC-done is the gate; ADR-182 proposed covers auto-transition on merge; ADR-195 accepted 2026-05-21 extends with follow-up Task transfer. Ghostbuster wire-up to query kanban is a skill-side concern at `plugins/atmux/skills/ghostbuster/scripts/ghostbuster.sh`, not a core verb gap. Re-file if a NEW hit surfaces past these surfaces.)
- c-aa586493 → **already-addressed** (sopx-driver, severity=high, safety-gap: "dissolve-epic must verify branch is merged-to-trunk before destruction". `src/verbs/team/dissolve-epic.ts:580-605` `deleteMergedEpicBranch` runs `merge-base --is-ancestor` REGARDLESS of `--skip-checks`; the `skipChecks` param does NOT bypass the guard. Branch preserved on unmerged-commit detection. Operator's near-miss scenario `e-5145f17a +4` was already in trunk via fan-in cascade — existing check would correctly return `merged=true`. The "refuse-the-dissolve" vs "log-and-preserve-branch" semantic preference is behavioral, not a safety gap; re-file as enhancement if predictability becomes blocking.)
- c-ca2b6b90 → **filed task t-c56e016e** (sopx-driver, severity=med, design-gap: "Default spawn-epic to --no-init-submodules; lead handles submodule init as first task". Real implementation drift surfaced: ADR-088 §Decision accepted 2026-05-13 specifies opt-in default `false`, but `src/verbs/team/spawn-epic.ts:113` ships `let initSubmodules = true;` — opposite of policy. Task aligns code to existing ADR + same-commit updates lead bootstrap brief to include submodule-init as first concrete task. Operator's existing CLAUDE.md workaround `ATMUX_SPAWN_TIMEOUT_MS=120000` becomes optional once spawn is structural-only.)
- c-800b0a95 → **filed task t-6210e753** (sopx-driver, severity=med, safety-gap: "spawn-epic not transactional — partial failures leave half-state blocking retries". Real bug: `spawn-epic.ts:436-499` rollback EXISTS but only fires when `provisionWorktree` returns successfully (`needsRollback = provision.created` is set AFTER the await). When `provisionWorktree` throws mid-submodule-init, worktree dir is on disk but `needsRollback` stays false → finally block skips rollback → retry blocked by "already exists" at :394. Task wraps the provisionWorktree call in try/catch with `exists(epicRoot)` check + branch -D + re-throw. Companion to t-c56e016e (which eliminates the most common trigger); deps t-c56e016e.)
- c-6445c932 → **filed epic t-67f9061a** (sopx-driver, severity=high, safety-gap: "spawn-epic incremental mode renames windows but does NOT restart claude processes — identity drift". Silent dispatch-chain break: `start.ts:679` skips on canonical-window-exists, `:697/:713` emoji-rename-in-place preserves PID per ADR-135 §D4 / ADR-161 TR2 (correct for emoji migration, wrong for identity change). Operator's case: 6 cages had lead-window renamed to `🐝-lead` but inside processes still had `ATMUX_MEMBER=be-2`. Epic decomposes into start.ts auto-promote-to-force on drift detection + new doctor `cage-member-identity-drift` probe + bau `⚙️ Identity-Drift` verdict + new ADR. Fifth sopx-driver complaint today on spawn-dissolve-respawn cycle.)

## Doctor regressions (optional)

## Notes (optional)
