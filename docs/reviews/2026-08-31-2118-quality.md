# Quality gate — e-1e8991a8 Meta-number window hotkeys

Measured at: 2026-08-31 21:18 MYT

Commit: `d49e8a2c feat(tmux): add Meta digit window hotkeys`
Overall result: **FAIL**

The hotkey implementation and its focused real-tmux integration test pass. The repository's full readiness gate remains red, so this batch is not eligible for deployment.

## Gate measurements

| Gate | Result | Evidence |
| --- | --- | --- |
| Lint | PASS | `bun run lint` exited 0. Biome emitted 180 warnings and 31 infos with no errors; timezone and socket-resolver guards passed. |
| Typecheck | PASS | `bun run typecheck` exited 0. |
| Format | FAIL | `biome format .` exited 1 with 98 errors across pre-existing repository files. Targeted format and lint checks for the four task-touched files passed. Existing owner: `t-5fd962f9`. |
| Focused real-tmux integration | PASS | Unsandboxed `env -u TMUX bun test tests/regression/atmux-conf-meta-hotkeys.test.ts`: 3 passed, 0 failed, 65 assertions against tmux 3.7c. The test loads the shipped config into an isolated real tmux server and proves `prefix M-0..M-9` maps to windows 10..19, built-in `prefix 1..9` remains intact, no root-table Meta-digit binding exists, and the local override stays last. |
| Full test + coverage run | FAIL | Sandboxed `env -u TMUX bun run test:coverage`: 9,995 passed, 444 failed, 11 errors, 9 skipped, 2 todo; 10,450 tests across 363 files. Failures include pre-existing docs/plugin assertions plus sandbox-denied tmux, loopback-server, home-directory, and HAX legs. The two real-tmux cases in the new file fail only in this sandbox run; the focused unsandboxed receipt above is green. Existing test-debt owner: `t-26160b99`. |
| Strict tracked-path coverage | FAIL | `bun tests/lcov-gate.ts`: 228 breaches across 309 tracked files. Existing coverage owner: `t-74-ad77b680`. |
| Feature e2e | FAIL | No attached terminal client drove the prefix and Meta-digit sequence. The real tmux-process test is integration coverage, not e2e. **No e2e coverage.** |

## Review receipt

Four fresh adversarial review rounds examined the implementation. The first two rejected missing source-level binding/override assertions and an incomplete root-table syntax guard; both defects were corrected. The third reviewer accepted the corrected implementation, and a fourth fresh reviewer accepted the final formatted and typechecked batch with no findings.

## Feature evidence and gap

- `templates/tmux/atmux.conf` binds `M-0` through `M-9` in tmux's `prefix` key table to window indexes 10 through 19.
- `tests/regression/atmux-conf-meta-hotkeys.test.ts` checks the source contract and loads the shipped configuration in a real isolated tmux process.
- `README.md` and `CHANGELOG.md` document the mapping and keep indexes 20+ on chooser or explicit selection.
- The final local override remains after the default bindings, so operators can replace or remove any mapping.
- The feature has no e2e coverage because no real terminal client sent the user-visible prefix and Meta-key sequence.

## Required next actions

1. Drain repository-wide formatting debt in `t-5fd962f9` without weakening the formatter.
2. Drain the full-suite failures tracked by `t-26160b99` and make sandbox-dependent legs deterministic or explicitly provisioned.
3. Restore the repository's 100% tracked-path coverage gate under `t-74-ad77b680` without weakening thresholds.
4. Add an isolated attached-terminal client test if the repository gains a deterministic harness for real Meta-key input; until then, keep reporting no e2e coverage.
5. Re-run every gate before deployment.
