# Quality gate — t-73-ab348668 folder-trust bootstrap

Measured at: 2026-08-31 19:11 MYT; live-test amendment at 2026-08-31 19:39 MYT

Commits: `7cb79f9b fix(bootstrap): accept first-run folder trust safely`; `3c4fa0e0 test(e2e): cover Claude folder trust live path`
Overall result: **FAIL**

The focused implementation is reviewed and its unit tests pass, but the repository's full readiness gate is red. The commit has not been pushed or deployed.

## Gate measurements

| Gate | Result | Evidence |
| --- | --- | --- |
| Lint | PASS | `bun run lint` exited 0. Biome emitted 180 warnings and 31 infos with no errors; timezone and socket-resolver guards passed. |
| Typecheck | PASS | `bun run typecheck` exited 0. |
| Format | FAIL | `bunx biome format .` exited 1 and reached Biome's 100-diagnostic cap across pre-existing repository files. The three task-touched files are clean under targeted Biome format/lint checks. Follow-up: `t-5fd962f9`. |
| Focused unit tests | PASS | `env -u TMUX bun test tests/unit/core/boot-claude.test.ts`: 52 passed, 0 failed, 166 assertions. |
| Full test + coverage run | FAIL | On semantic-equivalent predecessor `3651d32c`, `env -u TMUX bun run test:coverage`: 9,946 passed, 480 failed, 6 errors, 8 skipped, 2 todo; 10,436 tests across 359 files. Before the later opt-in e2e-substitute test was added, the only implementation changes were Biome whitespace corrections in touched files; the 52 focused tests were rerun on `7cb79f9b`. The new test's opt-in-off path was then checked separately. Full-suite failures include stale ADR/plugin assertions and sandbox-denied HAX, tmux, home-directory, and loopback-server legs. Existing test-debt owner: `t-26160b99`. |
| Strict tracked-path coverage | FAIL | `bun tests/lcov-gate.ts`: 241 breaches across 308 tracked files, including 6 tracked files with no coverage record. `src/core/boot-claude.ts` measured 238/243 lines (97.94%) and 22/23 functions (95.65%); uncovered lines 303–307 are the pre-existing `pollUntil` catch. Existing coverage owner: `t-74-ad77b680`. |
| Feature e2e | FAIL | `tests/e2e/boot-claude-folder-trust-live.test.ts` adds an opt-in live integration/e2e substitute through the production `start()` member-boot path with real tmux and Claude, but it calls the verb directly and injects observation plus cockpit isolation, so it is not full CLI e2e. The authenticated live run was not executed because it requires explicit operator approval to consume a Claude request and write folder-trust state. This feature has no executed e2e coverage. |

## Review receipt

Five fresh adversarial review rounds examined the trust-modal implementation. Four rejected concrete race or capture-tail defects; those defects were corrected. The fifth fresh reviewer accepted the final code and the 52-test focused suite. After a formatter-only amendment, the same focused suite and targeted lint/format checks remained green.

A separate fresh adversarial reviewer accepted `tests/e2e/boot-claude-folder-trust-live.test.ts` with no findings and classified it as a live integration/e2e substitute rather than full CLI e2e. Independent non-live checks passed: targeted Biome format, targeted Biome lint, TypeScript, `git diff --check`, and the opt-in-off test path (one intentional skip, zero failures). The live path remains untested.

## Feature evidence and gap

- Production callers: `src/verbs/start.ts` and `src/verbs/rotate.ts` call `bootClaudeMember(...)`.
- Exact modal recognition and bounded one-Enter handling: `src/core/boot-claude.ts`.
- Synthetic unit coverage: `tests/unit/core/boot-claude.test.ts` exercises exact, near-match, raced-away, persistent-modal, send-error, and deadline-edge cases.
- Wiring-level unit coverage: `tests/unit/verbs/rotate.test.ts` proves Claude rotation routes through `bootClaudeMember(...)` with a stub tmux harness.
- Opt-in live integration/e2e substitute: `tests/e2e/boot-claude-folder-trust-live.test.ts` delegates to real tmux while observing exact modal, bare-Enter, and boot-signal events; it remains unexecuted.
- Non-covering e2e evidence:
  - `tests/e2e/lifecycle.bats` uses `tui=shell` and never launches Claude.
  - `tests/e2e/cockpit-rotate.test.ts` uses a stub `claude` executable.
  - `tests/e2e/cage-state-transitions.test.ts` explicitly skips `atmux start` to avoid launching the real Claude TUI.

## Required next actions

1. After explicit operator approval for authenticated Claude use, execute `ATMUX_E2E_LIVE=1 env -u TMUX bun test tests/e2e/boot-claude-folder-trust-live.test.ts` and record the live receipt. A full CLI e2e remains preferable because the present test calls `start()` directly.
2. Drain repository-wide formatting debt in `t-5fd962f9` without semantic rewrites.
3. Drain the full-suite failures tracked by `t-26160b99` and make sandbox-dependent legs deterministic or explicitly provisioned.
4. Restore the repository's 100% tracked-path coverage gate under `t-74-ad77b680` without weakening thresholds.
5. Re-run every gate before push or deployment.
