// src/abstractions/spawn.ts — Bun.spawn wrapper with timeout, stderr capture, typed errors (ADR-007).
// Implementation lands in Phase 1 (foundation porter). Only this module may call Bun.spawn directly.
export {};
