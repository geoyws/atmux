{
  "name": "atmux",
  "version": "0.8.12",
  "type": "module",
  "private": true,
  "bin": {
    "atmux": "./bin/atmux",
    "atmux-bun": "./bin/atmux-bun"
  },
  "engines": {
    "bun": ">=1.3.13",
    "tmux": ">=3.4"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "test:coverage": "bun test --coverage",
    "lint": "biome lint . && bun scripts/lint-tz.ts && bun scripts/lint-socket-resolver.ts",
    "lint:tz": "bun scripts/lint-tz.ts",
    "lint:socket-resolver": "bun scripts/lint-socket-resolver.ts",
    "format": "biome format --write .",
    "build": "bun build ./bin/atmux-entry.ts --target=bun --outdir=dist",
    "build:compile": "bun build ./bin/atmux-entry.ts --compile --target=bun-linux-x64 --outfile dist/atmux",
    "build:listener": "cd rust/atmux-listener && cargo build --release",
    "build:orchd": "cd rust/atmux-orchd && cargo build --release",
    "build:cockpit-mirror": "cd rust/atmux-cockpit-mirror && cargo build --release",
    "build:install": "bun run build:compile && bun run build:listener && bun run build:orchd && bun run build:cockpit-mirror && sudo install -d -m 755 /opt/atmux/$npm_package_version/bin && sudo install -m 755 dist/atmux /opt/atmux/$npm_package_version/bin/atmux && sudo install -m 755 rust/atmux-listener/target/release/atmux-listener /opt/atmux/$npm_package_version/bin/atmux-listener && sudo install -m 755 rust/atmux-orchd/target/release/atmux-orchd /opt/atmux/$npm_package_version/bin/atmux-orchd && sudo install -m 755 rust/atmux-cockpit-mirror/target/release/atmux-cockpit-mirror /opt/atmux/$npm_package_version/bin/atmux-cockpit-mirror && sudo ln -sfn /opt/atmux/current/bin/atmux-orchd /usr/local/bin/atmux-orchd && sudo ln -sfn /opt/atmux/current/bin/atmux-cockpit-mirror /usr/local/bin/atmux-cockpit-mirror && sudo rm -rf /opt/atmux/$npm_package_version/templates && sudo cp -r templates /opt/atmux/$npm_package_version/templates && sudo rm -rf /opt/atmux/$npm_package_version/plugins && sudo cp -r plugins /opt/atmux/$npm_package_version/plugins && sudo ln -sfn /opt/atmux/$npm_package_version /opt/atmux/current && sudo ln -sfn /opt/atmux/current/bin/atmux /usr/local/bin/atmux"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.14",
    "@types/bun": "^1.3.13",
    "typescript": "^6.0.3"
  }
}
