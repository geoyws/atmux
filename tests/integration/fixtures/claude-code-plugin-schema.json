{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "_pinNote": {
    "pinnedFor": "Claude Code 2.1.146 (verified 2026-05-21 on hax)",
    "purpose": "Frozen-copy of Claude Code's plugin manifest schema as derived from (a) observation of working plugins under ~/work/journals/.sb/claude-skills/plugins/ on CC 2.1.146 and (b) memory feedback_reload_plugins_before_assuming_skill_missing — CC 2.1.143 broke plugins via userConfig schema bump (type∈{string|number|boolean|directory|file}, title required).",
    "rotationDiscipline": "When users hit a red /doctor row after a CC upgrade, refresh this file + plugins/atmux/.claude-plugin/plugin.json together in an ADR-217 amendment commit. CI will fail this test until the fixture + the actual plugin.json agree on the new shape.",
    "spec": "Subset of full JSON Schema draft-07 we recognise: type / required / properties / additionalProperties / items / enum / anyOf / minLength."
  },
  "type": "object",
  "required": ["name", "version", "description"],
  "additionalProperties": true,
  "properties": {
    "name": { "type": "string", "minLength": 1 },
    "version": { "type": "string", "minLength": 1 },
    "description": { "type": "string", "minLength": 1 },
    "atmuxCompat": { "type": "string", "minLength": 1 },
    "author": {
      "anyOf": [
        { "type": "string" },
        {
          "type": "object",
          "additionalProperties": true,
          "properties": {
            "name": { "type": "string" },
            "email": { "type": "string" },
            "url": { "type": "string" }
          }
        }
      ]
    },
    "homepage": { "type": "string" },
    "license": { "type": "string" },
    "keywords": { "type": "array", "items": { "type": "string" } },
    "skills": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["name", "path"],
        "additionalProperties": true,
        "properties": {
          "name": { "type": "string", "minLength": 1 },
          "path": { "type": "string", "minLength": 1 }
        }
      }
    },
    "userConfig": {
      "type": "object",
      "additionalProperties": {
        "type": "object",
        "required": ["type", "title"],
        "additionalProperties": true,
        "properties": {
          "type": { "enum": ["string", "number", "boolean", "directory", "file"] },
          "title": { "type": "string", "minLength": 1 },
          "description": { "type": "string" },
          "default": {},
          "required": { "type": "boolean" }
        }
      }
    }
  }
}
